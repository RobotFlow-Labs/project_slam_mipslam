version: "3.8"
services:
  susanoo:
    build: .
    volumes:
      - /Volumes/AIFlowDev/RobotFlowLabs/datasets:/data:ro
    environment:
      - ANIMA_BACKEND=cuda
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
