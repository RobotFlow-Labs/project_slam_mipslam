[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "anima-susanoo"
version = "0.1.0"
description = "ANIMA SUSANOO — MipSLAM: Anti-Aliased 3DGS SLAM"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.0",
    "numpy",
    "opencv-python",
]

[project.optional-dependencies]
mlx = ["mlx>=0.10", "mlx-nn"]
dev = ["pytest", "ruff", "mypy"]

[tool.ruff]
line-length = 100
target-version = "py310"
