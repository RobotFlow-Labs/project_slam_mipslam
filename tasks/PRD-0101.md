# Task PRD-0101: Replace Placeholder Scaffold

> PRD: PRD-01 | Depends: None
> Files: 4 | Lines: ~260 | Time: ~75min

## What to Build
Create the canonical `anima_slam_mipslam` package, move version/device/config entrypoints under it, and leave a temporary shim for the current `anima_susanoo` placeholder so the repo stays importable during migration.

## Files
### Create: `src/anima_slam_mipslam/__init__.py`
```python
from .version import __version__
from .device import get_backend, get_device
```

### Create: `src/anima_slam_mipslam/version.py`
```python
__version__ = "0.1.0"
```

### Create: `src/anima_slam_mipslam/device.py`
```python
def get_backend() -> str:
    ...
```

## Verify
```bash
uv run python -c "import anima_slam_mipslam; print(anima_slam_mipslam.__version__)"
```
