# Task PRD-0102: Implement Replica And TUM RGB-D Loaders

> PRD: PRD-01 | Depends: PRD-0101
> Files: 4 | Lines: ~420 | Time: ~90min

## What to Build
Add dataset loaders that emit RGB, depth, intrinsics, poses, and resolution-scale metadata for the exact evaluation regimes used in the paper.

## Files
### Create: `src/anima_slam_mipslam/data/replica.py`
```python
class ReplicaSequence(torch.utils.data.Dataset):
    def __getitem__(self, index: int) -> FrameBatch:
        ...
```

### Create: `src/anima_slam_mipslam/data/tum_rgbd.py`
```python
class TUMRGBDSequence(torch.utils.data.Dataset):
    def __getitem__(self, index: int) -> FrameBatch:
        ...
```

### Create: `tests/test_datasets.py`
```python
def test_replica_scale_protocol():
    ...
```

## Verify
```bash
uv run pytest tests/test_datasets.py -v
```
