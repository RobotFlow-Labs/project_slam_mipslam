# Task PRD-0103: Add Gaussian And Pose Graph Types

> PRD: PRD-01 | Depends: PRD-0101
> Files: 3 | Lines: ~280 | Time: ~60min

## What to Build
Define the typed state objects that every later PRD will consume: Gaussian primitives, map state, frame batch, keyframe record, and pose graph edges with spectral metadata placeholders.

## Files
### Create: `src/anima_slam_mipslam/gaussians/types.py`
```python
@dataclass
class GaussianPrimitive:
    mean: torch.Tensor
    covariance: torch.Tensor
    opacity: torch.Tensor
    sh_coeffs: torch.Tensor
```

### Create: `tests/test_geometry.py`
```python
def test_gaussian_covariance_is_3x3():
    ...
```

## Verify
```bash
uv run pytest tests/test_geometry.py -v
```
