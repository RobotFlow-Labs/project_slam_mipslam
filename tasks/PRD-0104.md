# Task PRD-0104: Wire Config And Evaluation Protocols

> PRD: PRD-01 | Depends: PRD-0102, PRD-0103
> Files: 4 | Lines: ~220 | Time: ~60min

## What to Build
Create the project settings model plus benchmark protocol configs for Replica and TUM. Include explicit scale lists and dataset roots so reproduction runs are deterministic.

## Files
### Create: `src/anima_slam_mipslam/config.py`
```python
class EvalProtocol(BaseModel):
    dataset: str
    scales: list[str]
```

### Create: `configs/eval/replica.toml`
```toml
scales = ["2x", "1x", "1/2", "1/4", "1/8"]
```

### Create: `tests/test_config.py`
```python
def test_eval_protocol_parses():
    ...
```

## Verify
```bash
uv run pytest tests/test_config.py -v
```
