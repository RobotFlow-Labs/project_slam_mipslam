# Task PRD-0201: Implement Incremental 3D Filter

> PRD: PRD-02 | Depends: PRD-0103
> Files: 2 | Lines: ~180 | Time: ~45min

## What to Build
Encode Eq. (3) as the per-Gaussian frequency support update driven by active keyframes and observed depth.

## Files
### Create: `src/anima_slam_mipslam/mapping/filter3d.py`
```python
def update_sampling_frequency(nu_hat, focal_length, depths):
    return torch.maximum(nu_hat, focal_length / depths)
```

### Create: `tests/test_filter3d.py`
```python
def test_sampling_frequency_is_monotonic():
    ...
```

## Verify
```bash
uv run pytest tests/test_filter3d.py -v
```
