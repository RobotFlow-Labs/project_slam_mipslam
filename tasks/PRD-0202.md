# Task PRD-0202: Implement Elliptical-Domain Sampling

> PRD: PRD-02 | Depends: PRD-0201
> Files: 2 | Lines: ~240 | Time: ~75min

## What to Build
Project 2D Gaussian covariances into their principal-axis basis, compute condition numbers, and sample the elliptical domain with boundary-aware importance weighting from Eq. (5).

## Files
### Create: `src/anima_slam_mipslam/render/eaa_sampling.py`
```python
def sample_elliptical_domain(mu_2d, sigma_2d, pixel_box):
    eigvals, eigvecs = torch.linalg.eigh(sigma_2d)
    ...
```

### Create: `tests/test_eaa_sampling.py`
```python
def test_sampling_weights_favor_high_condition_number():
    ...
```

## Verify
```bash
uv run pytest tests/test_eaa_sampling.py -v
```
