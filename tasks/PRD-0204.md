# Task PRD-0204: Build SA-PGO Descriptors And Spectral Signatures

> PRD: PRD-02 | Depends: PRD-0102
> Files: 3 | Lines: ~280 | Time: ~75min

## What to Build
Implement the multi-modal descriptor stack from Eq. (8) and the sliding-window trajectory DFT from Eq. (9)-(12).

## Files
### Create: `src/anima_slam_mipslam/backend/descriptors.py`
```python
def compute_descriptor(rgb, depth) -> torch.Tensor:
    return torch.cat([fft_feats, grad_feats, texture_feats, color_feats], dim=0)
```

### Create: `src/anima_slam_mipslam/backend/frequency.py`
```python
def spectral_signature(poses: torch.Tensor) -> torch.Tensor:
    ...
```

### Create: `tests/test_sa_pgo_descriptors.py`
```python
def test_descriptor_has_expected_modalities():
    ...
```

## Verify
```bash
uv run pytest tests/test_sa_pgo_descriptors.py -v
```
