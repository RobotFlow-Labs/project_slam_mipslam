# Task PRD-0205: Implement Pose Graph Optimization And Losses

> PRD: PRD-02 | Depends: PRD-0204
> Files: 3 | Lines: ~320 | Time: ~90min

## What to Build
Assemble the normalized graph Laplacian, spectral confidence weights, optimization target from Eq. (14), and the RGB-depth loss from Eq. (15).

## Files
### Create: `src/anima_slam_mipslam/backend/sa_pgo.py`
```python
def optimize_pose_graph(poses, edges, descriptors):
    ...
```

### Create: `src/anima_slam_mipslam/training/losses.py`
```python
def joint_loss(rgb_pred, rgb_gt, depth_pred, depth_gt):
    return lambda_rgb * l_rgb + lambda_depth * l_depth
```

### Create: `tests/test_sa_pgo.py`
```python
def test_laplacian_gap_controls_edge_pruning():
    ...
```

## Verify
```bash
uv run pytest tests/test_sa_pgo.py tests/test_losses.py -v
```
