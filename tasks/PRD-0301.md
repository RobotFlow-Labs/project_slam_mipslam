# Task PRD-0301: Implement Tracking Front-End

> PRD: PRD-03 | Depends: PRD-0203, PRD-0205
> Files: 2 | Lines: ~240 | Time: ~75min

## What to Build
Create the per-frame tracking step that uses rendered RGB-depth supervision against incoming RGB-D observations to estimate the next pose.

## Files
### Create: `src/anima_slam_mipslam/tracking/frontend.py`
```python
class TrackingFrontend:
    def track(self, frame, map_state, pose_init):
        ...
```

### Create: `tests/test_tracking_frontend.py`
```python
def test_tracking_step_returns_pose_matrix():
    ...
```

## Verify
```bash
uv run pytest tests/test_tracking_frontend.py -v
```
