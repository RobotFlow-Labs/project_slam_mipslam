# Task PRD-0304: Add CLI And Render-Reuse Paths

> PRD: PRD-03 | Depends: PRD-0303
> Files: 3 | Lines: ~220 | Time: ~60min

## What to Build
Add command-line entrypoints for sequence runs, map export, and arbitrary-resolution re-rendering from saved checkpoints.

## Files
### Create: `src/anima_slam_mipslam/cli/run_slam.py`
```python
@app.command()
def run_sequence(config: Path):
    ...
```

### Create: `tests/test_render_reuse.py`
```python
def test_saved_map_renders_at_new_resolution():
    ...
```

## Verify
```bash
uv run pytest tests/test_render_reuse.py -v
```
