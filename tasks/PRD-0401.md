# Task PRD-0401: Implement Rendering Metrics

> PRD: PRD-04 | Depends: PRD-0304
> Files: 2 | Lines: ~180 | Time: ~45min

## What to Build
Compute PSNR, SSIM, and LPIPS per scene and per scale so the Replica and TUM tables can be regenerated.

## Files
### Create: `src/anima_slam_mipslam/eval/render_metrics.py`
```python
def compute_render_metrics(pred, gt) -> dict[str, float]:
    ...
```

### Create: `tests/test_eval_metrics.py`
```python
def test_psnr_ssim_lpips_keys_present():
    ...
```

## Verify
```bash
uv run pytest tests/test_eval_metrics.py -v
```
