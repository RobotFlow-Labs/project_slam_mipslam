# Task PRD-0501: Define API Contracts

> PRD: PRD-05 | Depends: PRD-0304
> Files: 3 | Lines: ~220 | Time: ~60min

## What to Build
Define FastAPI request and response schemas for map building, rendering, relocalization, and export.

## Files
### Create: `src/anima_slam_mipslam/api/schemas.py`
```python
class RenderRequest(BaseModel):
    map_path: Path
    height: int
    width: int
```

### Create: `src/anima_slam_mipslam/api/app.py`
```python
app = FastAPI()
```

### Create: `tests/test_api_contract.py`
```python
def test_render_schema_requires_resolution():
    ...
```

## Verify
```bash
uv run pytest tests/test_api_contract.py -v
```
