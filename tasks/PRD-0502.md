# Task PRD-0502: Add Docker Packaging

> PRD: PRD-05 | Depends: PRD-0501
> Files: 2 | Lines: ~140 | Time: ~45min

## What to Build
Containerize the API and worker paths with mounted dataset/model volumes and a single reproducible entrypoint.

## Files
### Create: `docker/Dockerfile.api`
```dockerfile
FROM python:3.11-slim
```

### Create: `docker/docker-compose.api.yml`
```yaml
services:
  mipslam-api:
    build:
      context: ..
```

## Verify
```bash
docker compose -f docker/docker-compose.api.yml config
```
