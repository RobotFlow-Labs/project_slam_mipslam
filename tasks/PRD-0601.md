# Task PRD-0601: Add ROS Message Conversions

> PRD: PRD-06 | Depends: PRD-0301
> Files: 2 | Lines: ~180 | Time: ~45min

## What to Build
Convert ROS2 RGB, depth, and camera-info messages into the canonical `FrameBatch` used by the online pipeline.

## Files
### Create: `src/anima_slam_mipslam/ros2/conversions.py`
```python
def ros_to_frame_batch(rgb_msg, depth_msg, camera_info_msg) -> FrameBatch:
    ...
```

### Create: `tests/test_ros2_conversions.py`
```python
def test_ros_to_frame_batch_shape_contract():
    ...
```

## Verify
```bash
uv run pytest tests/test_ros2_conversions.py -v
```
